const numbers = [88, 77, 99, 421, 54, 63];

console.log(numbers);

console.log(numbers[0]); // for showing particular value or index of the array.
console.log(numbers[1]); // for showing particular value or index of the array.
console.log(numbers[2]); // for showing particular value or index of the array.

const fourth = numbers[3]; // for access specific index value set.
console.log(fourth);

// set or update element value by index
let abc = 45;
abc = 99;

numbers[1] = 69;
console.log(numbers); // set and showing the output of the update.